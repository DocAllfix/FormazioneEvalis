// Client Drizzle (Postgres / Supabase EU). Placeholder strutturale.
// L'inizializzazione del pool e l'export di `db` vengono aggiunti allo Step 1
// (Supabase + Drizzle + prima migrazione). Vedi ARCHITETTURA.md §4.

export {};
