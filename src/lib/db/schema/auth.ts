// Cluster A — Auth/Tenant (ARCHITETTURA.md §3)
// Tabelle generate/gestite da better-auth (organization plugin):
// user, session (+active_organization_id, +single-session), account,
// organization (metadata.type = personal|company, campi Stripe), member,
// invitation, organization_role.
//
// Le definizioni Drizzle verranno aggiunte allo Step 2 (auth/tenant)
// dell'ordine di implementazione. Placeholder strutturale.

export {};
