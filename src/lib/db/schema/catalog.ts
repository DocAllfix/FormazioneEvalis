// Cluster B — Catalogo & Fruizione (ARCHITETTURA.md §3) [BUILD]
// course, module, lesson (activity_type video|scorm|document, video_uid,
// min_required_seconds), enrollment (source b2b_seat|b2c_purchase, expires_at?),
// lesson_progress (effective_watch_seconds, max_validated_seconds, completed_at?),
// quiz_question, quiz_attempt (locked_until?).
//
// Ogni record porta organization_id. Definizioni Drizzle allo Step 3.

export {};
